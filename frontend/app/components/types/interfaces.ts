
export interface Schedule {
    id: number;
    address: string;
    totalTokens: string;
    startTime: string;
    endTime: string;
    status: string;
}

export interface ActivityDataItem {
    name: string;
    value: number;
}